@page
@model BrindarSuplementosModel
@{
    ViewData["Title"] = "BrindarSuplementos";
}

<center>
    <div>
        <form method="POST"
              enctype="multipart/form-data">
            @Html.AntiForgeryToken()

            <div align="left">
                <h3>Brindar Suplementos</h3>
            </div>

            <div>
                <input type="submit"
                       value="Refrescar"
                       class="btn btn-secondary"
                       asp-page-handler="BtRefrescar" />
                <input type="submit"
                       value="Nuevo"
                       class="btn btn-secondary"
                       asp-page-handler="BtNuevo"
                       disabled="@(Model.BrindarSuplemento != null)" />
                <input type="submit"
                       value="Guardar"
                       class="btn btn-secondary"
                       asp-page-handler="BtGuardar"
                       disabled="@(Model.BrindarSuplemento == null)" />
            </div>

            @if (Model.BrindarSuplemento != null)
            {
                <input type="hidden"
                       asp-for="@Model.BrindarSuplemento!.Id" />

                <div>
                    <div class="form-group">
                        <label>
                            Fecha brindar suplemetos
                        </label>
                        <div class="col-6">
                            <input id="inFechaBrindarSuplemetos"
                                   class="cts_forms form-control"
                                   asp-for="@Model.BrindarSuplemento!.FechaBrindarSuplemetos" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>
                            Id del Animal
                        </label>
                        <div class="col-6">
                            <input id="inIdAnimal"
                                   class="cts_forms form-control"
                                   asp-for="@Model.BrindarSuplemento!.IdAnimal" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>
                            Id del Suplemento
                        </label>
                        <div class="col-6">
                            <input id="inSuplementoId"
                                   class="cts_forms form-control"
                                   asp-for="@Model.BrindarSuplemento!.SuplementoId" />
                        </div>
                    </div>
                   

                    
                </div>
            }

            @if (ViewData.ContainsKey("Mensaje"))
            {
                @await Html.PartialAsync("../Emergentes/Mensajes.cshtml")
                ;
            }

            @if (Model.BrindarSuplemento != null && Model.Borrando)
            {
                <div class='popup'>
                    <div class='pp_ms_inner'>
                        <div style='width:95%'>
                            <h2 align="left">
                                Mensaje
                            </h2>

                            <h6 align="left">
                                Desea borrar el BrindarSuplemento?
                            </h6>
                            <h6 align="left">
                                @Model.BrindarSuplemento!.FechaBrindarSuplemetos
                            </h6>

                            <input type="submit"
                                   value="Cerrar"
                                   asp-page-handler="BtCerrar"
                                   class="btn btn-secondary" />
                            <input type="submit"
                                   value="Borrar"
                                   asp-page-handler="BtBorrar"
                                   class="btn btn-secondary" />
                        </div>
                    </div>
                </div>
            }

            @if (Model != null &&
                Model.ListaBrindarSuplementos != null &&
                Model.ListaBrindarSuplementos.Count > 0)
            {
                <table class="tb_data table table-striped table-bordered table-hover">
                    <tr>
                        <th>
                            Fecha brindar suplemetos
                        </th>
                        <th>
                            Id del Animal
                        </th>
                        <th>
                            Id del suplemento
                        </th>
                       

                        <th class="cl_actions">
                            Acción
                        </th>
                    </tr>
                    @foreach (var elemento in Model.ListaBrindarSuplementos)
                    {
                        <tr>
                            <td>
                                @elemento.FechaBrindarSuplemetos.ToString("dd/MM/yyyy")
                            </td>
                            <td>
                                @elemento.IdAnimal
                            </td>
                            <td>
                                @elemento.SuplementoId
                            </td>
                           

                            <td>
                                <input type="submit"
                                       value="Modicar"
                                       asp-page-handler="BtModificar"
                                       class="btn btn-secondary"
                                       asp-route-data="@elemento.Id" />
                                <input type="submit"
                                       value="Borrar"
                                       asp-page-handler="BtBorrarVal"
                                       class="btn btn-secondary"
                                       asp-route-data="@elemento.Id" />
                            </td>
                        </tr>
                    }
                </table>
            }
        </form>
    </div>
</center>
